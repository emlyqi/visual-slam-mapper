sequence_id: "05"
data_dir: "data/kitti/05"
gt_path: "data/kitti/05/poses/05.txt"

# VO params
keyframe_translation_threshold: 1.0
keyframe_rotation_threshold_deg: 5.0
n_features: 2000

# Vocabulary params
vocab_k: 1000
vocab_max_descriptors: 1000000

# Loop detection params
loop_top_k: 5
loop_min_bow_score: 0.5
loop_temporal_window: 20
loop_min_inliers: 20
loop_reproj_threshold: 2.0

# Pose graph params
odom_trans_sigma: 0.1
odom_rot_sigma_deg: 2.0
loop_trans_sigma_base: 1.0
loop_rot_sigma_deg_base: 10.0
loop_inlier_ref: 100